using UnityEngine;

public class npc : MonoBehaviour
{
    public bool talkable = false;
    public string name = "";
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
